{
  "extends": [
    "@council/eslint-config"
  ], 
  "rules": {
    // Testnet projects run in the terminal and are allowed to use console.log
    // for showing output things
    "no-console": "off"
  }
}
